# Prompt Templates for AI Code Generation

## Flask Route Template
Write a Flask route that:
- Accepts [method] at [endpoint]
- Validates input using [schema name]
- Delegates to [service name] for business logic
- Returns JSON with [response schema]
- Handles [error cases] with appropriate status codes

## React Component Template
Create a React component that:
- Fetches data from [API endpoint]
- Displays [data visualization/type]
- Handles [user interaction]
- Shows loading/error states

## Service Function Template
Write a service function that:
- Takes [parameters]
- Performs [business logic]
- Returns [result type]
- Raises [specific exceptions] on failure
